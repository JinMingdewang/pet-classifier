# 【科研项目组初步培训】基于深度学习的细粒度图像分类（3天极速挑战）
> **面向对象**：申请加入科研项目组的本科生（零基础/低基础友好 · 强调快速学习与实战执行力）  
> **考核周期**：**72 小时（3 天极速挑战）**  
> **核心原则**：充分利用 AI 编程工具提效，重在“快速跑通基线 ➔ 严谨完成对比 ➔ 讲清代码原理与实验结果”。

---

## 📌 目录
- [一、 考核目标与挑战要求](#一-考核目标与挑战要求)
- [二、 3天极速通关指南（72-Hour Sprint Protocol）](#二-3天极速通关指南72-hour-sprint-protocol)
  - [Day 1（破冰与基线）：环境就绪、数据加载与 Baseline 跑通](#day-1破冰与基线环境就绪数据加载与-baseline-跑通)
  - [Day 2（探索与消融）：1~2组单变量消融实验与特征可视化](#day-2探索与消融12组单变量消融实验与特征可视化)
  - [Day 3（交付与复盘）：模块化重构、精简报告与答辩准备](#day-3交付与复盘模块化重构精简报告与答辩准备)
- [三、 AI 辅助提效“秘籍”与自查避坑](#三-ai-辅助提效秘籍与自查避坑)
- [四、 极简技术报告模板（2 ~ 3 页）](#四-极简技术报告模板2--3-页)
- [五、 提交清单与评分细则](#五-提交清单与评分细则)
- [六、 答辩抽查自查清单（FAQ）](#六-答辩抽查自查清单faq)

---

## 一、 考核目标与挑战要求

在科研工作中，“**快速阅读文档、利用 AI 工具高效落地代码、快速排错并交付规范结果**”是极为重要的核心能力。本次 72 小时考核专为考查该项综合潜质设计：

* **任务数据集**：`Oxford-IIIT Pet`（牛津宠物数据集，37 类细粒度分类，约 7,349 张图片）。
* **训练耗时参考**：使用免费 GPU（Google Colab / Kaggle T4），ResNet-18 训练 10 轮仅需 **3 ~ 5 分钟**，整套实验 1 小时内即可跑完。
* **核心交付物**：
  1. 规范的 **GitHub 仓库**（含清晰 README 与一键复现命令）。
  2. 精炼的 **技术报告（2 ~ 3 页 PDF）**。
  3. 完整的 **训练曲线日志（TensorBoard 日志或 WandB 链接）**。

---

## 二、 3天极速通关指南（72-Hour Sprint Protocol）

```
【Day 1】环境配置 (1h) ➔ 数据加载 (2h) ➔ Baseline 训练跑通 (2h)
【Day 2】消融实验对比 (3h) ➔ 混淆矩阵与 Grad-CAM 可视化 (2h)
【Day 3】代码工程化封装 (2h) ➔ 撰写精简报告 (2h) ➔ 提交与答辩自查
```

---

### 🟢 Day 1（破冰与基线）：环境就绪、数据加载与 Baseline 跑通

> **今日目标**：完成环境准备，写出第一个可运行的 PyTorch 脚本，实现 80%+ 的分类准确率。

#### 1. 快速环境准备（推荐免费云端，零配置门槛）
* **强烈推荐**：直接使用 **Google Colab** 或 **Kaggle Notebook**，挂载免费 T4 GPU，免去本地复杂的驱动配置。
* **核心依赖安装**：
  ```bash
  pip install torch torchvision timm matplotlib pandas scikit-learn tensorboard
  ```

#### 2. 数据加载与分层划分
* 使用 `torchvision.datasets.OxfordIIITPet(root="./data", download=True)`。
* **数据切分**：按 **70% 训练 / 15% 验证 / 15% 测试** 进行**分层抽样（Stratified Split）**，确保每个类别均匀分布。
* **数据预处理**：
  * **训练集**：`Resize(256)` + `RandomCrop(224)` + `RandomHorizontalFlip()` + `Normalize`。
  * **验证/测试集**：`Resize(256)` + `CenterCrop(224)` + `Normalize`（**切记：测试集不可加随机翻转或随机裁剪！**）。

#### 3. 跑通 Baseline 训练循环
* **模型选择**：预训练骨干网络 `torchvision.models.resnet18(weights="DEFAULT")`，替换最后一层全连接层输出为 `37`。
* **训练超参数**：优化器 `AdamW(lr=1e-4)` 或 `SGD(lr=1e-3, momentum=0.9)`，损失函数 `nn.CrossEntropyLoss()`，Batch Size 32，训练 **10 ~ 15 个 Epoch**。
* **记录日志**：使用 TensorBoard 记录每轮的 Train Loss, Val Loss, Val Accuracy，并自动保存 `best_model.pth`。

#### 🎯 Day 1 达标检查点
- [ ] 打印出 DataLoader 输出的 Batch 形状 `[32, 3, 224, 224]`。
- [ ] Baseline 训练完成，验证集 Top-1 准确率达到 **80% 以上**。

---

### 🟢 Day 2（探索与消融）：1~2组单变量消融实验与特征可视化

> **今日目标**：控制单变量，完成有科研意义的对比实验，并生成直观的可视化图表。

#### 1. 完成 1 ~ 2 组消融实验（单变量控制法，任选 1~2 项）
| 实验方向 | 对照组 A (Baseline) | 实验组 B (改进方案) | 观察核心 |
| :--- | :--- | :--- | :--- |
| **方案 1：数据增强对比** | 基础增强 (Crop + Flip) | 进阶增强 (RandAugment 或 MixUp) | 是否显著减轻了过拟合？ |
| **方案 2：微调策略对比** | 仅微调全连接层 (Freeze Backbone) | 全参数微调 (Full Fine-tuning) | 训练收敛速度与最终精度的权衡 |
| **方案 3：损失函数对比** | 标准 CrossEntropyLoss | Label Smoothing CrossEntropy (0.1) | 对外观极其相似的品种分类收益如何？ |

#### 2. 模型分析与可视化（两张关键图）
1. **混淆矩阵（Confusion Matrix）**：绘制 37 类的预测热力图，找出最容易被模型搞混的 2~3 对猫狗品种。
2. **Grad-CAM 热力图可视化（加分亮点）**：选取 2 张典型图片（一张预测正确，一张预测错误），观察网络重点关注了宠物的头部、耳朵还是身体斑纹。

#### 🎯 Day 2 达标检查点
- [ ] 产出规范的实验对比数据（包含 Baseline 与改进组的 Top-1 Acc、Macro-F1）。
- [ ] 保存混淆矩阵图与 2 张 Grad-CAM 特征热力图。

---

### 🟢 Day 3（交付与复盘）：模块化重构、精简报告与答辩准备

> **今日目标**：将代码整理成规范的工程项目，撰写 2~3 页高质量精简报告。

#### 1. 代码模块化重构（禁止提交单文件乱序 Notebook）
将代码整理为清晰的 Python 目录结构：
```text
pet_project/
├── data/
│   └── dataset.py       # 数据加载、划分与 Transform
├── models/
│   └── model.py         # 模型定义与 Head 替换
├── utils/
│   ├── metrics.py       # 准确率、F1、混淆矩阵绘制
│   └── gradcam.py       # Grad-CAM 可视化工具
├── train.py             # 训练主入口（支持 argparse 参数控制）
├── evaluate.py          # 评估与生成可视化结果的独立脚本
├── requirements.txt     # 依赖清单
└── README.md            # 项目说明与一键复现命令
```

#### 2. 撰写技术报告（2 ~ 3 页精简版）
对照下文 [极简技术报告模板](#四-极简技术报告模板2--3-页) 快速成稿并导出 PDF。

#### 🎯 Day 3 达标检查点
- [ ] 按照 README.md 里的命令，测试能否在一台全新环境/Colab 会话中一键复现实验。
- [ ] GitHub 仓库推送完毕，PDF 报告排版完成。

---

## 三、 AI 辅助提效“秘籍”与自查避坑

在 72 小时内，高效使用 AI（ChatGPT / Claude / Cursor / Copilot）是取胜关键，但必须防范常见逻辑错误：

### 1. 高效 Prompt 提问模板
* **写数据加载时**：  
  > “请用 PyTorch 为 Oxford-IIIT Pet 数据集编写 `dataset.py`。要求：1. 使用 `torchvision` 自动下载；2. 按照 70:15:15 进行分层抽样划分；3. 训练集做 Resize/RandomCrop/Flip，验证集做 Resize/CenterCrop；4. 固定随机种子 `seed=42`。代码需模块化并附带类型注解。”
* **写消融模块时**：  
  > “请在 PyTorch 现有训练循环中加入 Label Smoothing CrossEntropy 损失函数，并支持在 `train.py` 中通过 `--label_smoothing 0.1` 参数开关控制。”

### 2. 必须自查的 4 个 AI 常见“暗坑”
1. ❌ **验证集发生数据泄漏**：检查验证/测试集的 Transform 是否混入了 `RandomHorizontalFlip` 或 `RandomCrop`。
2. ❌ **评估时漏写模式切换**：检查验证函数中是否有 `model.eval()` 和 `with torch.no_grad():`。
3. ❌ **梯度未清零**：检查 `optimizer.zero_grad()` 是否在 `loss.backward()` 之前调用。
4. ❌ **Loss 重复 Softmax**：`nn.CrossEntropyLoss()` 自带 Softmax，模型输出层**严禁**再加一层 `nn.Softmax()`。

---

## 四、 极简技术报告模板（2 ~ 3 页）

> **排版建议**：结构紧凑，多用“表格 + 图表”，少堆砌冗长废话。

```markdown
# 基于深度学习的牛津宠物细粒度分类实验报告
姓名：XXX  |  学号：XXX  |  GitHub：https://github.com/xxx/pet-classifier

## 1. 任务背景与实验设置
- 数据集概况（37类，约7.3k张）与 7:1.5:1.5 分层划分说明。
- 硬件环境与基础超参数配置（ResNet-18, AdamW, lr=1e-4, Batch Size=32, 15 Epochs）。

## 2. 实验结果与消融分析
- 【对比表格】：
  | 实验配置 | Top-1 Acc (%) | Top-5 Acc (%) | Macro-F1 | 训练轮数/耗时 |
  | :--- | :--- | :--- | :--- | :--- |
  | 1. Baseline (ResNet-18 基础增强) | xx.x% | xx.x% | 0.xxx | 15 ep / 4min |
  | 2. 改进组 A (+ MixUp 数据增强) | xx.x% | xx.x% | 0.xxx | 15 ep / 4min |
  | 3. 改进组 B (+ Label Smoothing) | xx.x% | xx.x% | 0.xxx | 15 ep / 4min |
- 【曲线分析】：展示 Train/Val Loss 与 Accuracy 收敛曲线，简要分析改进策略的有效性。

## 3. 错误案例分析（Bad Case）与 Grad-CAM 可视化
- 展示 1 张混淆矩阵局部图，指出最易混淆的品种（如 `Staffordshire Bull Terrier` vs `American Pit Bull`）。
- 展示 2 张 Grad-CAM 热力图，图文说明模型关注的关键特征区域。

## 4. AI 辅助编程声明与 Bug 复盘
- 说明在哪些环节使用了 AI 工具（如生成 Grad-CAM 代码、编写训练循环等）。
- 记录 1 个 AI 代码中发现的 Bug 及排查修复过程。
```

---

## 五、 提交清单与评分细则

### 📦 提交清单（截止时间前提交）
1. **GitHub 仓库链接**（代码结构清晰、含规范 Commit 记录与 README）。
2. **技术报告 PDF**（命名：`【考核】姓名_学号_宠物分类.pdf`，2~3 页）。
3. **训练曲线日志**（TensorBoard 日志文件夹或在线 WandB 链接）。

### 📊 评分细则（满分 100 分）

| 评分维度 | 权重 | 评分要点 |
| :--- | :--- | :--- |
| **工程代码规范** | 25分 | 模块化架构（20分）、README 清晰度与一键复现（5分） |
| **实验严谨与消融** | 30分 | Baseline 完整跑通（15分）、1~2 组严谨单变量消融（15分） |
| **分析与报告质量** | 25分 | 图表规范（10分）、混淆矩阵/Grad-CAM 深入分析（10分）、AI 复盘真实性（5分） |
| **答辩与代码理解** | 20分 | 能清楚解释关键代码原理与 Tensor 维度，思路清晰 |

---

## 六、 答辩抽查自查清单（FAQ）

在提交前，请确保你能自信回答以下 4 个问题（10分钟线上答辩重点）：

1. **维度理解**：输入一张 `[32, 3, 224, 224]` 的 Batch，经过 ResNet-18 的最后一层卷积层（在 AvgPool 前）输出的 Tensor 形状是多少？（答：`[32, 512, 7, 7]`）
2. **实验规范**：为什么在验证集和测试集上不能使用 `RandomCrop` 和 `RandomHorizontalFlip`？（答：保持测试环境的确定性和客观性，避免引入随机干扰）
3. **现象诊断**：如果训练 Loss 很低但验证 Loss 飙升，代表什么？你在实验中用了什么方法缓解？（答：过拟合；通过数据增强、Weight Decay 或 Early Stopping 缓解）
4. **原理掌握**：请解释 `optimizer.zero_grad()` 为什么要放在每个 Batch 训练循环的开头？（答：PyTorch 默认会累积梯度，每次迭代必须显式清空上一步的梯度）
